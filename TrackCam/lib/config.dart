class AppConfig {
  /// Base URL for the backend API that provides signed upload URLs.
  /// Replace this with your actual backend URL.
  static const String apiBaseUrl = 'https://your-api.example.com';
}
