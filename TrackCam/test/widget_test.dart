import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Camera-dependent app requires device; smoke test is a placeholder.
    expect(true, isTrue);
  });
}
