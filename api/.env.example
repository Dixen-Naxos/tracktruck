# MongoDB connection (both optional; defaults shown)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=hackathon

# Path to a Firebase Admin service account JSON file (required)
FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json

# Cloud Storage bucket name, e.g. my-project.appspot.com (required)
FIREBASE_STORAGE_BUCKET=
